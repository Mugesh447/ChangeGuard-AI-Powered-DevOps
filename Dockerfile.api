FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY server ./server
RUN mkdir -p /app/data
ENV NODE_ENV=production
EXPOSE 4000
VOLUME ["/app/data"]
CMD ["node", "server/index.js"]
